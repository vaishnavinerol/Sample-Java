pipeline {
    agent any


    stages {
        stage("Stage 1") {
            steps {
                script {

                    env.BAR = "sub_multi1"
					echo "${BAR}"
                }
            }
        }

        stage("Stage 2") {
            steps {
                script {
                    echo "${BAR}"
                    env.BAR = "sub_multi1"
					echo "${BAR}"
					sh '''
					ABC="wxs"
					DJSD=echo"${ABC}"
					
					'''
					
					
                }
            }
        }

        
    }
}
